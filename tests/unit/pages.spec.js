import { mount } from '@vue/test-utils'
import { About, Blog, Contact, Index, Services, Targets} from '../../src/pages'

describe('About.vue', () => {
  test('setup correctly', () => {
    expect(true).toBe(true)
  })
})
